## BACKEND

1. First start the backend by going to the appstore-reviews folder and running ```go run ./cmd/server```

2. Open another terminal and go to the frontend and run ``` pnpm install && pnpm dev``` to start the frontend

